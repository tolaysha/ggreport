# Sprint Report CLI - Environment Configuration
#
# This project uses TWO environment files:
#   .env        - Non-sensitive configuration (can be committed)
#   .env.local  - Secrets only (NEVER commit this file!)
#
# Setup:
#   1. Copy this file to .env:     cp env.example.txt .env
#   2. Create .env.local:          touch .env.local
#   3. Add your secrets to .env.local (see bottom of this file)
#
# IMPORTANT: .env.local overrides values from .env

# =============================================================================
# .env - Non-sensitive config (safe to commit)
# =============================================================================

# General
MOCK_MODE=true

# Jira (public metadata)
JIRA_BASE_URL=https://unimatch.atlassian.net
JIRA_EMAIL=anatoliikuzmins@gmail.com
JIRA_BOARD_ID=1
JIRA_ARTIFACT_FIELD_ID=customfield_10001

# Notion (non-secret ID)
NOTION_PARENT_PAGE_ID=1a52e8c0cf694f67b363dc17754d922a

# OpenAI (model selection)
OPENAI_MODEL=gpt-4o

# =============================================================================
# .env.local - Secrets ONLY (NEVER commit!)
# =============================================================================
# Create a separate .env.local file and add these values:
#
# JIRA_API_TOKEN=your_jira_api_token_here
# NOTION_API_KEY=your_notion_integration_secret_here
# OPENAI_API_KEY=your_openai_api_key_here
#
# Get your tokens:
#   - Jira API token: https://id.atlassian.com/manage-profile/security/api-tokens
#   - Notion API key: https://www.notion.so/my-integrations
#   - OpenAI API key: https://platform.openai.com/api-keys
